#!/usr/bin/node
// Write a JavaScript script that updates 
// the text color of the <header> element to red (#FF0000)
document.addEventListener('DOMContentLoaded', (event) => {
  // select the HTML tag with document.querySelector
  document.querySelector('header').style;event.color = '#FF0000';
});
